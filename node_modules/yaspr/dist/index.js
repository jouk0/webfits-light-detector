"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.sequentialPromises = void 0;
/**
 * Will call the supplied function sequentially for all of the supplied parameters.
 * @param funcParams An array of parameters for the function. Function is called in the order of these parameters.
 * @param func Function to call sequentially.
 */
function sequentialPromises(funcParams, func) {
    return __awaiter(this, void 0, void 0, function* () {
        const results = [];
        yield funcParams.reduce((prev, params) => __awaiter(this, void 0, void 0, function* () {
            yield prev;
            results.push(yield func(params));
        }), Promise.resolve());
        return results;
    });
}
exports.sequentialPromises = sequentialPromises;
